#include <stdio.h>

/**
 * main - printing uppercase and lowercase alphabets
 *
 * Return: Aways 0
 */

int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
	putchar('\n');

	return (0);
}
